import { SortChannelsPipe } from './sort-channels.pipe';

describe('SortChannelsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortChannelsPipe();
    expect(pipe).toBeTruthy();
  });
});
