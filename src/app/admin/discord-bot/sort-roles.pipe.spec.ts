import { SortRolesPipe } from './sort-roles.pipe';

describe('SortRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
