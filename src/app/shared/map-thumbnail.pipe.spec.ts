import { MapThumbnailPipe } from './map-thumbnail.pipe';

describe('MapThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new MapThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
