import { SafeMumbleUrlPipe } from './safe-mumble-url.pipe';

describe('SafeMumbleUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeMumbleUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
